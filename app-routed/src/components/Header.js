import React from "react"

import { Jumbotron } from "reactstrap"

const Header = () => (
  <div>
    <h1 className="display-6">Hello, React Router!</h1>
    <p className="lead">Simple way to use routing in React</p>
  </div>
)

export default Header
